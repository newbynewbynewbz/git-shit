.DS_Store
*.log
.claude/settings.local.json

# Self-install artifacts — if someone accidentally runs `bash install.sh`
# from the git-shit source repo, these files get written at the root.
# They're byproducts, not source — ignore them so they don't clutter git status.
/.gitshitrc
/.gitattributes
/.gitmessage
/.github/
/scripts/
